# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Python — only ignore caches and venv, NOT data files
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.egg

# Local env
.env
.env.local

# Result files that are regenerated by running compute_capture_ratios.py
# (These are reproducible from data/raw/, so re-generating is fine.)
# KEEP data/raw/ and results/ in version control — they ARE the artifact.
